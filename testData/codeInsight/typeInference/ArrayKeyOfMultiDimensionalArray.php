<?php
/**
 * @param array<int, array<string, class-string<Exception>>> $param
 */
function f($param){
    foreach ($param as <type value="int">$key</type>=>$value){
        foreach ($value as $kk => $value) {

        }
    }
}